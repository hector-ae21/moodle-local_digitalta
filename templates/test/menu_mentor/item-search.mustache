{{#mentors}}
<div class="list-tutoring-group-item d-flex justify-content-between align-items-center mb-2">
  <div class="d-flex align-items-center">
    <div class="position-relative">
      <img src="{{profile_img}}" class="tutoring_profile_image rounded-circle" />
    </div>
    <div class="ml-3">
      <h6 class="mb-0">{{name}}</h6>
      <small>{{#str}}tutoring:at_university, local_dta{{/str}} {{university}}</small>
    </div>
  </div>
  <div>
    <button class="btn btn-outline-primary" data-toggle="tooltip" data-placement="top" title="Send request">
      <i class="fa fa-user-plus"></i>
    </button>
    <button class="btn btn-outline-danger" data-toggle="tooltip" data-placement="top" title="Cancel sent request">
      <i class="fa fa-user-times"></i>
    </button>
  </div>
</div>
{{/mentors}}
{{^mentors}}
<div class="text-center">
  <h5>No results found</h5>
</div>
{{/mentors}}