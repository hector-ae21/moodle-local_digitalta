<div class="mb-5">
  {{#pictureurl}}
    <div class="experience-card-img-container card-img-top border-bottom">
      <img
        src="{{pictureurl}}"
        alt="{{#str}} myexperience_picture_alt, local_dta {{/str}}"
        class="img-fluid experience-card-img"
      />
    </div>
  {{/pictureurl}}
  {{#showimageprofile}}
    <!-- avatar -->
    {{#user}}
      <div class="avatar avatar-xl mt-n7 ms-4">
      <a href="{{profileurl}}" class="shadow-none">
        <img src="{{imageurl}}" alt="User Image" class="rounded-circle border-4 border-white-color-40"/>
      </a>
      </div>
    {{/user}}
  {{/showimageprofile}}
  <div class="experience-card-body">
    <h4 class="mb-1 experience-card-title">
    <a href="{{experiences.viewurl}}{{id}}">{{title}}</a>
    </h4>
    <a href="{{experiences.viewurl}}{{id}}">
    <div class="experience-description mb-3">{{{description}}}</div>
    </a>
      {{#reactions}}
        {{> local_dta/reactions-buttons}}
      {{/reactions}}
  </div>
</div>
