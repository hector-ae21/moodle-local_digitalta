<div class="container-fluid">
<input type="hidden" id="ourcases-id" value="{{ourcase.id}}">
  <div class="row">
    <!-- Sidebar -->
    <div class="col-md-3 sidebar">
      <h4>Experiencia:</h4>
      <hr />
      <h5>{{experience.title}}</h5>
      <p>{{experience.description}}</p>
    </div>
    <div class="col-md-9">
      <div class="row">
        <div class="col-md-12" id="sections">
          {{#section_header}} {{> local_dta/ourcases/section_header }}
          {{/section_header}} {{#sections}} {{> local_dta/ourcases/section_text
          }} {{/sections}}
        </div>
        <div class="col-md-12">{{>local_dta/ourcases/section_add }}</div>
      </div>
    </div>
  </div>
</div>